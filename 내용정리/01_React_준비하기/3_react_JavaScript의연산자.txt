기본 사칙연산이나 비교연산과 같은 것들은 이미 알고 있다고 생각하고 넘어간다.

JavaScript 에서는 '일치연산자' 라는 것이 있는데 다음과 같이 생겼다.
===, !==

'===' 연산자는 비교하려는 변수들끼리 값과 자료형이 모두 같다면 true 를 반환한다.
'!==' 연산자는 비교하려는 변수들끼리 값 또는 자료형이 하나라도 다르다면 true 를 반환한다.

예시)
```
    let a = 1;
    let b = '1';

    a == b --> true
    a === b --> false
```